import 'package:flutter/material.dart';

class WorkoutScreen2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Workout Screen 2'),
      ),
      body: Center(
        child: Text('Content for Workout Screen 2'),
      ),
    );
  }
}
