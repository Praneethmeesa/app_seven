import 'package:flutter/material.dart';

class WorkoutScreen7 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Workout Screen 7'),
      ),
      body: Center(
        child: Text('Content for Workout Screen 7'),
      ),
    );
  }
}
